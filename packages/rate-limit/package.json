{
  "name": "@repo/rate-limit",
  "version": "2.0.0",
  "private": true,
  "description": "Redis-based rate limiting for Brumkit",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@upstash/redis": "catalog:",
    "ioredis": "catalog:"
  },
  "devDependencies": {
    "@repo/config-typescript": "workspace:*",
    "@repo/config-vitest": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
